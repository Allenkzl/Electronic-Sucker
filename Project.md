# 电子吸盘浏览器控制器 — 项目全记录

> 用浏览器网页点「吸取 / 放下」，通过 Arduino Uno 控制电子吸盘与电磁阀完成取放料动作。

| | |
|---|---|
| 仓库 | https://github.com/Allenkzl/Electronic-Sucker |
| 硬件 | Arduino Uno（CH340 兼容板）+ 电子吸盘（气泵）+ 电磁阀 |
| 代码规模 | 约 1900 行（固件 329 / 桥接 622 / 网页 528 / 工具 393） |
| 固件占用 | Flash 6494 B（20%），RAM 403 B（19%） |
| 运行环境 | macOS + Python 3.11（仅需 pyserial）+ Chrome/Safari |

---

## 目录

1. [项目概述](#1-项目概述)
2. [硬件层](#2-硬件层)
3. [系统架构](#3-系统架构)
4. [端到端运行逻辑](#4-端到端运行逻辑)
5. [固件设计](#5-固件设计)
6. [桥接服务设计](#6-桥接服务设计)
7. [前端设计](#7-前端设计)
8. [关键设计决策记录](#8-关键设计决策记录)
9. [缺陷记录](#9-缺陷记录)
10. [测试体系与验证结果](#10-测试体系与验证结果)
11. [部署与使用](#11-部署与使用)
12. [扩展指南与已知限制](#12-扩展指南与已知限制)

---

## 1. 项目概述

### 1.1 要解决的问题

厂家提供的参考代码把动作时序**写死在 `setup()` 里**，上电后只能自动循环 10 次
（吸 1 秒 → 放 0.8 秒），既不能手动触发，也不能和上层业务联动。

本项目把它改造成**常驻命令驱动**的控制器：Arduino 一直待命，由网页发指令决定何时吸、何时放。

### 1.2 成品形态

打开 `http://127.0.0.1:8787`，界面上有：

- **吸取** / **放下** 两个大按钮，以及红色**急停**
- 顶部三个状态灯：服务 / 串口 / 设备
- 两盏实时状态灯：吸盘（气泵）、电磁阀
- 参数面板：吸取保持时长、放气脉冲时长、看门狗超时、输出模式
- 运行日志

### 1.3 分层的职责

| 层 | 文件 | 职责 |
|---|---|---|
| 硬件 | — | 接收舵机式 PWM 信号，驱动气泵与电磁阀 |
| 固件 | `firmware/sucker/sucker.ino` | 输出 PWM、执行动作时序、看门狗保护、状态上报 |
| 桥接 | `bridge/bridge.py` | 串口收发、HTTP/SSE 服务、自动重连、心跳 |
| 前端 | `web/index.html` | 交互界面、实时状态渲染 |

---

## 2. 硬件层

### 2.1 接线

| Arduino Uno | 接到 | 信号含义 |
|---|---|---|
| **A0**（数字引脚 14） | 吸盘 / 气泵 模块信号线 | `180` = 吸，`0` = 停 |
| **A2**（数字引脚 16） | 电磁阀 模块信号线 | `180` = 进气放料，`0` = 关 |
| **GND** | 外部电源负极 | **必须共地** |

气泵与电磁阀由**外部电源独立供电**，只把信号线和 GND 接到 Arduino。

### 2.2 控制信号原理：为什么是 Servo 而不是 digitalWrite

这是整个项目最关键的一个判断。

厂家参考代码用的是：

```cpp
#include <Servo.h>
Servo servo_8;              // 引脚 8 = 电磁阀
Servo servo_9;              // 引脚 9 = 气泵
servo_9.write(180);         // 气泵开
servo_8.write(180);         // 电磁阀开
servo_X.write(0);           // 关
```

它**没有用 `digitalWrite`**，而是用舵机库输出 PWM。据此判定：
模块的控制输入是**舵机式 PWM 信号**（周期 20 ms，高电平 0.5~2.5 ms 对应 0°~180°），
而不是简单的高低电平。所以：

- 固件必须沿用 `Servo` 库输出，才能驱动厂家模块；
- 如果误用 `digitalWrite(HIGH)`，模块收到的是持续高电平，行为完全不同。

### 2.3 引脚选择：为什么 A0/A2 可以用

Uno 的 A0~A5 是**模拟输入引脚，但同时也是数字引脚 14~19**。
`Servo` 库在 AVR 上用 Timer1 产生脉冲，可以驱动任意数字引脚（最多 12 路），
因此 A0（14）和 A2（16）完全可以正常输出舵机信号。

代价：`Servo` 库会占用 Timer1，导致**引脚 9、10 的 `analogWrite` 失效**。
本项目不使用这两个引脚的 PWM，所以没有影响。

> 结论：**硬件接线一根都不用改**，只需把厂家代码里的引脚 8/9 改成 A0/A2，
> 并按实际接线把「阀」与「泵」的角色对调。

### 2.4 供电要求（最重要的一条）

气泵和电磁阀的工作电流通常在 **300 mA ~ 1 A**，远超 Arduino 板载稳压器的余量。

- ✅ 外部电源独立供电，GND 与 Arduino 共地
- ❌ **绝对不要**从 Arduino 的 5V 引脚给气泵/电磁阀供电

从板子取电的典型后果：稳压器过热、板子反复重启、USB 口保护甚至烧毁。

---

## 3. 系统架构

### 3.1 分层图

```
┌──────────────────────────────────────────────┐
│  浏览器网页  web/index.html                   │
│  吸取 / 放下 / 急停 · 状态灯 · 参数 · 日志      │
└───────────────┬──────────────────────────────┘
                │  HTTP (REST)  +  SSE (实时推送)
                │  绑定 127.0.0.1:8787，局域网不可访问
┌───────────────▼──────────────────────────────┐
│  桥接服务  bridge/bridge.py                   │
│  ├─ HTTP 线程池（ThreadingHTTPServer）         │
│  └─ 串口线程（收发 / 状态机 / 心跳 / 重连）      │
└───────────────┬──────────────────────────────┘
                │  串口 115200 8N1，文本行协议
                │  /dev/cu.wchusbserial1110
┌───────────────▼──────────────────────────────┐
│  固件  firmware/sucker/sucker.ino             │
│  命令解析 · 动作时序 · 看门狗 · 状态上报        │
└───────────────┬──────────────────────────────┘
                │  舵机式 PWM（180 = 开 / 0 = 关）
┌───────────────▼──────────────────────────────┐
│  A0 吸盘（气泵）        A2 电磁阀              │
└──────────────────────────────────────────────┘
```

### 3.2 一次点击经过的全部环节

```
用户点「吸取」
  → 前端 fetch POST /api/pick {pick_ms: 0}
  → 桥接 HTTP 线程：bridge.command("PICK 0")
  → 串口线程写入 "PICK 0\n"
  → 固件 handleLine()：先关电磁阀 → 开气泵 → 置 pumpOffAt
  → Servo.write(180) 输出 PWM
  → 气泵启动，吸住物体
  → 固件回 "{\"ok\":\"PICK\"}" 和 "{\"ev\":\"state\",\"pump\":1,...}"
  → 桥接串口线程解析，更新 state，通过 SSE 广播
  → 前端收到，吸盘状态灯变绿、按钮进入激活态
```

### 3.3 技术选型对比

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **Python 桥接（选用）** | pyserial 已装、零新增依赖；Chrome/Safari 通用；安全兜底可放服务端；可做自动重连、心跳、日志 | 多一个常驻进程 | **选用** |
| 浏览器 Web Serial API | 彻底无后端，一个静态页面即可 | 仅 Chrome/Edge；兜底逻辑只能在前端，页面一崩就失控；需要 secure context | 备选 |
| Node.js + serialport | 生态好 | 需要 npm 安装原生模块并编译 | 未采用 |

选 Python 的决定性因素是：**安全兜底必须在网页之外**。
气泵长时间空转是会烧的，把「断线自动关」的逻辑放在前端不可靠。

---

## 4. 端到端运行逻辑

### 4.1 启动：从上电到网页可用

```
① 上电 / 复位
   固件 setup()：
     - 先把 PIN_PUMP / PIN_VALVE 拉低（关键：避免一插电就吸住）
     - Serial.begin(115200)
     - setOutMode(MODE_SERVO) → attach 两个 Servo
     - stopAll() → 两路归零
     - 打印 "READY"、"{"ev":"boot",...}"、状态 JSON

② 启动桥接服务  ./scripts/run.sh
   - 扫描 /dev/cu.* 找串口（优先 wchusbserial / usbmodem）
   - 打开串口（打开瞬间原厂 Uno 会因 DTR 复位一次）
   - 握手：先补发一个 "PING"，认 "READY" 或 "PONG" 任一为就绪
   - 下发保存的参数：SET pick_ms / drop_ms / watchdog，再 MODE
   - 进入主循环：读串口 + 每 1.5 秒发一次 PING 心跳

③ 浏览器打开  http://127.0.0.1:8787
   - GET /api/status 拉一次全量状态并渲染
   - 建立 SSE 连接 GET /api/events，之后由服务端主动推
```

握手为什么要同时认 `READY` 和 `PONG`：
原厂 Uno 打开串口会因 DTR 复位并打印 `READY`；
但本机这块 **CH340 兼容板实测不复位**（`up` 计数连续增长可证），
所以补发 `PING`，收到 `PONG` 也算就绪。这样两种板子都能正确识别。

### 4.2 时序：点击「吸取」

| 时刻 | 前端 | 桥接 | 固件 |
|---|---|---|---|
| T+0 | `POST /api/pick {pick_ms:0}` | 收到，记日志 | |
| T+1ms | | 写 `PICK 0\n` | |
| T+3ms | | | `doPick(0)`：**先 `setValve(false)`**（否则真空被破坏）→ `setPump(true)` |
| T+4ms | | | `pumpOffAt = 0`（`pick_ms = 0` 表示一直保持） |
| T+5ms | | | 回 `{"ok":"PICK"}` + `{"ev":"state","pump":1,...}` |
| T+8ms | SSE 收到状态 | 解析并广播 | |
| T+10ms | 吸盘灯变绿、按钮进入 `.live` 激活态 | | |

**为什么先关电磁阀**：如果电磁阀开着，气路和大气连通，气泵抽不出真空，吸不住东西。

### 4.3 时序：点击「放下」

| 时刻 | 前端 | 桥接 | 固件 |
|---|---|---|---|
| T+0 | `POST /api/drop {drop_ms:800}` | 写 `DROP 800\n` | |
| T+3ms | | | `doDrop(800)`：**先 `setPump(false)`** → `setValve(true)` |
| T+4ms | | | `valveOffAt = millis() + 800` |
| T+803ms | | | 主循环检测到期 → `setValve(false)` → 上报 `valve:0` |

**为什么先关气泵**：先停泵再开阀，避免泵在放气的同时还在抽，动作干净。

**为什么是脉冲而不是常开**：电磁阀只是用来「进气破除真空」，放完料就没用了。
常开既费电又会让阀体发热。默认 800 ms 与厂家参考代码一致（实测 797 ms）。

### 4.4 时序：急停

三种触发方式：点按钮、按 `Esc`、桥接服务收到 SIGINT/SIGTERM（Ctrl+C 退出）。

前端只是发 `POST /api/stop`；真正兜底的是固件和桥接服务退出时的 `STOP`。

### 4.5 状态回传链路

固件**只在状态发生变化时**主动上报，不用前端轮询：

```
固件 reportState()  ──串口──▶  桥接 _handle_line()  ──SSE──▶  前端 applyState()
```

上报去重：固件用 `lastReportedPump` / `lastReportedValve` + `stateDirty` 标志，
避免每轮循环都刷同样内容。

前端还有一层降级：SSE 断了就自动切到每 1.5 秒轮询 `/api/status`，
且 `EventSource` 自身会重连。

### 4.6 异常路径

| 异常 | 后果 | 兜底机制 |
|---|---|---|
| 网页崩溃 / 关闭 | 设备保持当前状态（有意设计，避免误关网页掉件） | 需要时点「急停」 |
| 桥接服务崩溃（`kill -9`） | 心跳停止 | **固件看门狗**在超时后自动关闭两路 |
| 电脑休眠 | 心跳停止 | 同上 |
| USB 线拔掉 | 串口消失 | 固件看门狗；桥接服务每 2 秒重连，插回即恢复 |
| 串口被 Arduino IDE 占用 | 桥接服务打不开端口 | 日志给出明确提示，每 2 秒重试 |
| 开发板未烧固件 | 收不到 `READY`/`PONG` | 握手超时后继续尝试，界面「设备」灯不亮 |

---

## 5. 固件设计

文件：`firmware/sucker/sucker.ino`（329 行）

### 5.1 状态与变量

```cpp
const uint8_t PIN_PUMP  = A0;   // 吸盘（气泵），数字 14
const uint8_t PIN_VALVE = A2;   // 电磁阀，数字 16
const uint8_t PIN_LED   = 13;   // 板载 LED：亮 = 吸盘工作中（可空载目视验证）

const int ANGLE_OFF = 0;        // 关
const int ANGLE_ON  = 180;      // 开

bool     pumpOn, valveOn;                 // 当前输出状态
uint32_t pickMs     = 0;                  // 0 = 一直吸到 DROP
uint32_t dropMs     = 800;                // 与厂家代码一致
uint32_t watchdogMs = 15000;              // 0 = 关闭看门狗
unsigned long pumpOffAt, valveOffAt;      // 定时关闭时刻，0 = 不定时
unsigned long lastCmdAt;                  // 最近一次收到命令的时刻
```

### 5.2 主循环执行顺序（这一段有坑，见 §9.1）

```cpp
void loop() {
  // ① 串口收行（会更新 lastCmdAt）
  while (Serial.available() > 0) { ... handleLine(lineBuf); ... }

  // ② 取当前时间 —— 必须在 ① 之后！
  unsigned long now = millis();

  // ③ 定时关闭（吸取计时 / 放气脉冲计时）
  // ④ 看门狗
  // ⑤ 状态变化主动上报
}
```

**`now` 的位置至关重要**。如果放在 ① 之前，它会早于 `handleLine()` 里刚写入的
`lastCmdAt`，无符号相减下溢成约 43 亿，看门狗就会在刚收到命令的瞬间误触发。
详见 §9.1。

### 5.3 串口协议

115200 8N1，命令以 `\n` 结尾，不区分大小写。

| 命令 | 参数 | 作用 |
|---|---|---|
| `PICK` | `[ms]` | 吸盘开；`ms > 0` 到点自动关，缺省用 `pick_ms` |
| `DROP` | `[ms]` | 吸盘关 + 电磁阀脉冲进气放料 |
| `STOP` / `OFF` | — | 两路全关（急停） |
| `SET` | `pick_ms\|drop_ms\|watchdog N` | 参数设置 |
| `MODE` | `servo\|digital` | 输出模式 |
| `STATUS` | — | 回传一行状态 JSON |
| `PING` | — | 心跳，回 `PONG` |
| `HELP` | — | 打印命令列表 |

**回包格式**（全部为单行 JSON，便于桥接解析）：

```json
{"ok":"PICK"}                                            动作确认
{"ev":"state","pump":1,"valve":0,"pick_ms":0,"drop_ms":800,
 "watchdog":15000,"mode":"servo","up":54216}             状态上报
{"ev":"watchdog"}                                        看门狗触发
{"ev":"boot","fw":"sucker-1.0","pump_pin":"A0","valve_pin":"A2"}
{"err":"unknown command"}                                错误
READY / PONG                                             握手与心跳
```

### 5.4 输出模式 servo / digital

厂家模块用舵机信号，但存在「模块其实是两线通断型」的可能。
为了不用改线、不用重烧固件就能两种都试，固件内置两种输出模式：

| 模式 | 实现 | 适用 |
|---|---|---|
| `servo`（默认） | `Servo.write(180/0)` | 厂家模块（舵机式 PWM 输入） |
| `digital` | `digitalWrite(HIGH/LOW)`，并 `Servo.detach()` | 继电器 / MOS 管等两线通断模块 |

切换时会先把两路关掉再 detach，避免 Servo 的中断和 `digitalWrite` 抢同一个引脚。

### 5.5 安全机制

1. **上电即关闭**：`setup()` 里先把两个输出脚 `digitalWrite(LOW)`，再交给 Servo 库。
2. **看门狗**：超过 `watchdogMs` 没收到任何命令，且处于开启态 → 自动 `stopAll()`。
   这是防止上位机崩溃后气泵空转烧毁的最后一道防线，且完全由固件自主执行。
3. **行缓冲溢出保护**：命令超过 47 字节直接丢弃并回错误，不会越界。

---

## 6. 桥接服务设计

文件：`bridge/bridge.py`（622 行）。只用标准库 + pyserial，无 Web 框架。

### 6.1 线程模型

```
主线程 ── ThreadingHTTPServer.serve_forever()
   │
   ├─ HTTP 请求线程（每请求一个，daemon）
   │     ├─ 读 state：直接读 self.state（更新是原子的字典赋值）
   │     ├─ 写命令：bridge.command() → _send() → 拿 self.lock
   │     └─ SSE：每个连接一个线程，从自己的 Queue 取数据
   │
   └─ 串口线程（1 个，daemon）
         ├─ 打开串口 / 握手 / 下发参数
         ├─ readline() 收行 → _handle_line() → 更新 state → SSE 广播
         └─ 每 1.5 秒发一次 PING
```

- **写串口用 `self.lock` 串行化**，避免多个 HTTP 线程的命令交错写坏报文。
- **SSE 用 `queue.Queue` 广播**：每个订阅者一个队列，满了就丢弃（`put_nowait`），
  绝不因为一个慢客户端阻塞串口线程。

### 6.2 串口生命周期

```
                    ┌──────────────┐
                    │  扫描串口     │  没找到 → 等 2 秒重试
                    └──────┬───────┘
                           │ 找到
                    ┌──────▼───────┐
                    │  打开串口     │  失败（被占用等）→ 等 2 秒重试
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  握手         │  发 PING，等 READY 或 PONG（4 秒）
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  下发参数     │  SET pick_ms / drop_ms / watchdog、MODE
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  主循环       │  读行 + 心跳；出错 → 关闭 → 等 2 秒重连
                    └──────────────┘
```

串口自动探测优先级：`wchusbserial` / `usbmodem` > `usbserial` > 其他；
屏蔽掉 `Bluetooth`、`debug-console`、`wlan-debug` 等系统虚拟串口。

### 6.3 HTTP 接口

服务只绑定 `127.0.0.1`，局域网内其他设备无法访问。

| 方法 | 路径 | 请求体 | 作用 |
|---|---|---|---|
| GET | `/` | — | 控制网页 |
| GET | `/api/status` | — | 串口 + 设备实时状态（含 `stale` 标记） |
| GET | `/api/log` | — | 最近 60 条事件 |
| GET | `/api/events` | — | SSE 实时事件流 |
| POST | `/api/pick` | `{pick_ms}` | 吸取 |
| POST | `/api/drop` | `{drop_ms}` | 放下 |
| POST | `/api/stop` | — | 急停 |
| POST | `/api/settings` | `{pick_ms,drop_ms,watchdog,mode}` | 参数下发 + 持久化 |
| POST | `/api/mode` | `{mode}` | 单独切换输出模式 |

`/api/status` 返回：

```json
{"port":"/dev/cu.wchusbserial1110","connected":true,"ready":true,
 "pump":0,"valve":0,"pick_ms":0,"drop_ms":800,"watchdog":15000,
 "mode":"servo","last_error":null,"last_seen":1789112500.7,
 "server_time":1789112502.0,"stale":false}
```

### 6.4 SSE 推送

```
GET /api/events  →  持续连接
  data: {"type":"state","state":{...}}       连接建立时先推一次全量
  data: {"type":"log","event":{...}}         有事件时推
  : keepalive                                15 秒无数据时发注释行保活
```

选 SSE 而不是 WebSocket：数据是**单向**的（服务端 → 前端），
SSE 用标准库就能实现，不需要引入 `websockets` 依赖。

### 6.5 配置持久化与看门狗安全下限

参数保存在 `bridge/settings.json`，重启后仍然生效，并在每次串口握手成功后自动下发到固件。

**关键约束**：看门狗超时必须显著大于心跳间隔，否则桥接服务正常工作时也会被误判为失联。
因此设了 `MIN_WATCHDOG_MS = 6000`（心跳 1500 ms 的 4 倍）：

```python
def clamp_watchdog(value):
    v = max(0, int(value))
    if v == 0:
        return 0            # 0 = 显式关闭
    return max(MIN_WATCHDOG_MS, v)
```

用户设置小于下限的值时会被自动抬高，并在日志里给出警告。详见 §9.2。

---

## 7. 前端设计

文件：`web/index.html`（528 行）。**单文件、零构建**，双击即可读，改完刷新即生效。

### 7.1 结构

```
┌─ 品牌 + 三个状态灯（服务 / 串口 / 设备）─────────┐
├─ 告警条（串口打开失败时出现，附排查命令）────────┤
├─ ┌──────────┐ ┌──────────┐                    │
│  │  吸 取    │ │  放 下    │  两个大按钮         │
│  └──────────┘ └──────────┘                    │
├─ ┌──────────── 急 停 ─────────────┐             │
├─ ┌─吸盘（气泵）─┐ ┌─电磁阀─┐  实时状态灯         │
├─ 参数设置：吸取保持 / 放气脉冲 / 看门狗 / 输出模式 │
├─ 接线说明                                      │
├─ 运行日志                                      │
└─ 页脚：连接信息                                 │
```

视觉：暗色控制台风格，吸取用青绿、放下用琥珀、急停用红色。
响应式（<620 px 单列，手机上可用）、支持 `prefers-reduced-motion` 降级。

### 7.2 状态同步策略

| 通道 | 用途 |
|---|---|
| 页面加载时 `GET /api/status` | 拿到全量状态，首屏立即正确 |
| SSE `/api/events` | 后续所有状态与日志的实时推送 |
| SSE 断开 → 每 1.5 秒轮询 `/api/status` | 降级兜底 |
| `EventSource` 自身 | 自动重连 |

按钮可用性由 `connected && ready && !busy` 决定，避免在设备没就绪时误点。

表单同步时**跳过正在聚焦的输入框**（`document.activeElement !== node`），
否则用户正在输入数字时会被服务端状态回写打断。

### 7.3 交互

| 操作 | 快捷键 |
|---|---|
| 吸取 | `空格` |
| 放下 | `回车` |
| 急停 | `Esc` |

快捷键处理会跳过输入框内的按键，并对事件目标做类型保护（见 §9.3）。

参数改动后 **450 ms 防抖**再下发，避免连续输入时刷爆串口。

---

## 8. 关键设计决策记录

| # | 决策 | 理由 | 被否决的方案 |
|---|---|---|---|
| 1 | 沿用 `Servo` 库输出 PWM | 厂家模块收的是舵机信号，用 `digitalWrite` 行为完全不同 | 直接高低电平 |
| 2 | 保留 `MODE digital` 开关 | 模块类型存在不确定性，两种都能试且不用改线重烧 | 只实现一种 |
| 3 | 用 A0/A2 原引脚 | Uno 上 A0/A2 就是数字 14/16，硬件零改动 | 改接到 8/9 |
| 4 | 吸取默认「保持到放下」 | 符合真实取放料流程；定时模式仍可在界面调 | 照搬厂家「吸 1 秒」 |
| 5 | 放下用 800 ms 脉冲 | 放完料阀门就没用了，常开费电且发热；沿用厂家值 | 常开 |
| 6 | 放下时先关泵再开阀 | 动作干净，避免边抽边放 | 同时动作 |
| 7 | 吸取时先关阀再开泵 | 阀开着气路通大气，抽不出真空 | 不处理 |
| 8 | 固件内置看门狗 | 上位机崩溃时气泵不能一直空转烧掉 | 依赖宿主程序收尾 |
| 9 | 看门狗下限 6000 ms | 必须显著大于心跳 1500 ms，否则正常心跳也被误判 | 允许任意小值 |
| 10 | Python 桥接而非 Web Serial | 安全兜底必须在网页之外；跨浏览器 | Web Serial API |
| 11 | SSE 而非 WebSocket | 数据单向流动，标准库即可实现，零依赖 | WebSocket |
| 12 | 参数持久化到 JSON | 重启后不用重设；握手后自动下发到固件 | 只存内存 |
| 13 | 单文件前端、零构建 | 改完刷新即生效，无需 npm / 打包器 | React + Vite |
| 14 | 只绑定 127.0.0.1 | 控制物理设备，绝不暴露到局域网 | 0.0.0.0 |
| 15 | 工具链装在项目内 `.arduino15/` | 不污染 `~/Library/Arduino15`，不影响已有 Arduino IDE | 装到用户目录 |

---

## 9. 缺陷记录

以下三个是开发过程中**实测发现并修复**的真实缺陷，都记录在此以免复发。

### 9.1 固件看门狗无符号下溢（严重）

**症状**：点「吸取」后偶尔立刻自己松开；连续操作时随机复现，难以稳定重现。

**根因**：`loop()` 在**开头**取了 `unsigned long now = millis()`，
而串口命令是在那之后才由 `handleLine()` 处理的，`handleLine()` 把 `lastCmdAt`
写成了**比 `now` 更晚**的时间。于是：

```cpp
now - lastCmdAt        // 负数，但在 unsigned long 下溢成约 43 亿
  > watchdogMs         // 恒成立 → 看门狗立即触发
```

只要命令恰好落在**跨毫秒**的那一次循环迭代里就会触发，所以是随机复现的。

**修复**（两道保险）：

```cpp
// ① 把 now 的取值移到串口处理之后，保证 lastCmdAt <= now
while (Serial.available() > 0) { ... }
unsigned long now = millis();

// ② 看门狗改用有符号比较兜底，即使 lastCmdAt 意外晚于 now 也绝不误触发
if (watchdogMs > 0 && (pumpOn || valveOn) &&
    (long)(now - lastCmdAt) > (long)watchdogMs) { ... }
```

**回归测试**：`python3 tools/serial_test.py --stress 40`
连续 40 轮 PICK/DROP，精确统计 `{"ev":"watchdog"}` 出现次数，必须为 0。

> 这类 bug 在手动使用时会表现为「偶尔失灵」，很容易被误判成接触不良或电源问题。

### 9.2 看门狗超时与心跳间隔余量不足

**症状**：桥接服务正常运行，看门狗仍然触发。

**根因**：调试时把 `watchdog` 设为 4000 ms 并持久化到 `settings.json`，
而心跳间隔是 3000 ms，只剩 1 秒余量，稍有调度抖动就被误判为失联。

**修复**：心跳间隔降到 **1.5 s**；看门狗设 **6000 ms 安全下限**，
小于该值会被自动抬高并写警告日志（见 §6.5）。

### 9.3 前端 `e.target.matches is not a function`

**症状**：`Uncaught TypeError`，快捷键处理中断。

**根因**：`document.addEventListener('keydown', ...)` 里直接调用 `e.target.matches(...)`，
当事件目标恰好是 `document` 本身时它没有 `matches` 方法。

**修复**：

```js
const t = e.target;
if (t && typeof t.matches === 'function' && t.matches('input, select, textarea')) return;
```

顺带修了 `/favicon.ico` 返回 404 的问题（改为 204，保持控制台干净）。

### 9.4 教训：测试脚本自身的误报

排查 §9.1 时，第一版压力测试用子串匹配统计看门狗：

```python
if 'watchdog' in line:      # ❌ 错
```

但每条状态报文里都有 `"watchdog":15000` 这个**字段名**，于是把正常的状态上报
全部统计成了误触发，得出「30 轮误触发 92 次」的错误结论，一度误导了排查方向。

正确写法是精确匹配事件类型：

```python
WATCHDOG_MARK = '"ev":"watchdog"'    # ✅ 对
```

**教训**：断言的关键字必须唯一标识被测事件，否则测试本身就会撒谎。

---

## 10. 测试体系与验证结果

三层测试，从下到上覆盖整条链路。

### 10.1 固件层 — `tools/serial_test.py`

不经过网页，直接和固件对话。

```bash
python3 tools/serial_test.py --seq        # 跑一遍完整动作序列
python3 tools/serial_test.py --stress 40  # 看门狗回归压测
python3 tools/serial_test.py STATUS PING  # 发指定命令
```

实测结果：

```
  -> PING     <- PONG
  -> SET pick_ms 0     <- {"ok":"SET"}
  -> PICK     <- {"ok":"PICK"}  <- {"ev":"state","pump":1,"valve":0,...}
  -> DROP     <- {"ok":"DROP"}  <- {"ev":"state","pump":0,"valve":1,...}
                        797 ms 后  <- {"ev":"state","pump":0,"valve":0,...}
  -> STOP     <- {"ok":"STOP"}
```

放气脉冲实测 **797 ms**（设定 800 ms），符合预期。

### 10.2 安全机制 — 真实失效注入

不是模拟，而是真的把桥接服务 `kill -9` 硬杀（不执行任何清理逻辑），
此时没有任何程序能发 `STOP`：

```
硬杀前 吸盘 = 1
>>> kill -9 <pid>
7 秒后读取：{"ev":"state","pump":0,"valve":0,...,"up":178044}
```

**结果：气泵自动关闭**。且 `up` 计数连续，证明开发板全程未复位 ——
安全兜底完全由固件自主完成，与上位机无关。

### 10.3 浏览器层 — `tools/ui_test.py`

用 **Chrome DevTools Protocol** 真实加载页面、真实点击按钮，再回读设备状态，
覆盖「网页 → 桥接 → 串口 → 固件」整条链路，共 15 项断言：

```
== 1. 页面加载与界面状态 ==     6 项（连接状态、三个状态灯、三个按钮可用性）
== 2. 点击「吸取」 ==           3 项（设备 pump=1、界面灯变绿、按钮激活态）
== 3. 点击「放下」 ==           4 项（pump→0、valve→1、界面更新、脉冲后自动关）
== 4. 快捷键与急停 ==           2 项（再次吸取、Esc 触发急停）
== 5. 参数下发 ==               1 项（放气时长下发到设备）
== 6. 运行日志 ==               1 项（页面显示操作日志）
== 7. 前端异常 ==               1 项（无 JS 异常 / 控制台错误）
```

**结果：连跑 3 轮，全部通过。**

---

## 11. 部署与使用

### 11.1 首次安装

```bash
./scripts/setup.sh      # 下载 AVR 工具链 + Servo 库（约 250MB，只需一次）
                        # 装在项目内 .arduino15/，不影响已有的 Arduino IDE
```

> 注意：`arduino:avr` 1.8.8 起 **Servo 库不再随核心捆绑**，需要单独 `lib install Servo`，
> `setup.sh` 已经处理。

### 11.2 烧录固件

```bash
./scripts/flash.sh                          # 默认 /dev/cu.wchusbserial1110
./scripts/flash.sh /dev/cu.usbmodem14201    # 或指定串口
```

### 11.3 启动服务

```bash
./scripts/run.sh --open      # 启动并自动打开浏览器
```

访问 **http://127.0.0.1:8787** 。`Ctrl+C` 停止（停止时会先发 `STOP`）。

其他参数：

```bash
./scripts/run.sh --list                # 列出可用串口
./scripts/run.sh --port /dev/cu.xxx    # 手动指定串口
./scripts/run.sh --http-port 8899      # 换端口
./scripts/run.sh --host 0.0.0.0        # 需要局域网访问时（默认仅本机）
```

---

## 12. 扩展指南与已知限制

### 12.1 已知限制

- **页面关闭但服务仍在运行时，吸盘保持当前状态**。这是有意设计，避免误关网页就掉件；
  要立即松开请点「急停」。
- **打开串口时原厂 Uno 会因 DTR 复位一次**；本机 CH340 兼容板实测不复位，
  因此握手同时接受 `READY` 和 `PONG`。
- **看门狗最短 6000 ms**，这是为了和 1500 ms 心跳留足余量（见 §9.2）。
- 只支持单块开发板；没有做多设备管理。

### 12.2 常见扩展方向

| 想做什么 | 怎么做 |
|---|---|
| 调整吸取/放气时长 | 网页参数面板直接改，自动下发并保存；不用重烧固件 |
| 改接线引脚 | 改固件顶部 `PIN_PUMP` / `PIN_VALVE` 两个常量，重新 `flash.sh` |
| 动作方向相反 | 把固件里的 `ANGLE_ON` / `ANGLE_OFF` 对调（0 ↔ 180）重新烧录 |
| 模块是两线通断型 | 网页「输出模式」切 `digital`，不用改线 |
| 增加「吸住 N 秒后自动放」 | 前端调 `/api/pick {pick_ms:N}`，`pick_ms > 0` 即定时模式 |
| 接入自己的业务系统 | 直接调用 HTTP 接口；或写一个串口客户端发 `PICK` / `DROP` 文本命令 |
| 远程控制 | `--host 0.0.0.0` 暴露到局域网，**但务必自行加认证**，否则任何人都能控制你的设备 |
| 换成 ESP32 | 桥接层不用改（协议是文本行），只需把固件的 `Servo` 调用换成 `ledc` PWM |

### 12.3 给二次开发者的提醒

1. **改 `loop()` 时守住 §9.1 的教训**：`millis()` 的取值位置与时间比较的符号性。
2. **看门狗与心跳是一对**：改任意一个都要重新核算余量，下限由 `clamp_watchdog()` 保护。
3. **写测试断言时用唯一标识**：`"ev":"watchdog"` 而不是 `watchdog`（§9.4）。
4. **任何改动后至少跑一遍**：`serial_test.py --stress 40` + `ui_test.py`。

---

*文档随代码一同维护；改动行为时请同步更新本文档。*
